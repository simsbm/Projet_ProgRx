<Window x:Class="NetAdmin.Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="NetAdmin Pro - Dashboard" Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="White">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#673AB7" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" Margin="0,0,24,0">
                    <TextBlock Text="NETADMIN PRO" Foreground="White" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="Monitoring réseau &amp; contrôle distant" Foreground="#E6FFFFFF" FontSize="12"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border CornerRadius="12" Background="#20FFFFFF" Padding="10,4" Margin="0,0,12,0">
                        <TextBlock Text="Serveur actif" Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    <Border CornerRadius="12" Background="#20FFFFFF" Padding="10,4">
                        <TextBlock Text="Port 5000" Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F3F4F8">
                <StackPanel Margin="16">
                    <TextBlock Text="Navigation" FontWeight="SemiBold" Margin="4,0,0,12" Foreground="#666"/>

                    <Button Content="Vue d'ensemble" Padding="10,8" Margin="0,0,0,8" Background="#673AB7" Foreground="White" HorizontalAlignment="Stretch" Click="NavOverview_Click"/>
                    <Button Content="Clients actifs" Padding="10,8" Margin="0,0,0,8" Background="#673AB7" Foreground="White" HorizontalAlignment="Stretch" Click="NavClients_Click"/>
                    <Button Content="Journaux d'audit" Padding="10,8" Background="#673AB7" Foreground="White" HorizontalAlignment="Stretch" Click="NavAudit_Click"/>
                </StackPanel>
            </Border>

            <ScrollViewer x:Name="MainScrollViewer" Grid.Column="1" Padding="24" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="SectionOverview" Text="État du réseau" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>

                    <WrapPanel>
                        <Border Width="240" Padding="16" Margin="0,0,16,16" Background="White" BorderBrush="#DDD" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Clients en ligne" FontSize="12"/>
                                <TextBlock x:Name="TxtAvgCpu" Text="0" FontSize="24" FontWeight="Bold" Foreground="#673AB7"/>
                                <TextBlock Text="dernière mesure" Foreground="#888" FontSize="11"/>
                            </StackPanel>
                        </Border>

                        <Border Width="240" Padding="16" Margin="0,0,16,16" Background="White" BorderBrush="#DDD" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Dernier client" FontSize="12"/>
                                <TextBlock x:Name="TxtAvgRam" Text="-" FontSize="18" FontWeight="Bold" Foreground="#2E7D32"/>
                                <TextBlock Text="nom de machine" Foreground="#888" FontSize="11"/>
                            </StackPanel>
                        </Border>

                        <Border Width="240" Padding="16" Margin="0,0,16,16" Background="White" BorderBrush="#DDD" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Charge moyenne" FontSize="12"/>
                                <TextBlock Text="--" FontSize="24" FontWeight="Bold" Foreground="#FF8F00"/>
                                <TextBlock Text="en attente de données" Foreground="#888" FontSize="11"/>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <!-- Graphiques temps réel -->
                    <TextBlock Text="Métriques temps réel" FontSize="14" FontWeight="SemiBold" Margin="0,16,0,12"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border BorderBrush="#DDD" BorderThickness="1" Background="White" Margin="0,0,8,0">
                            <lvc:CartesianChart x:Name="CpuChart" Height="250" Margin="8">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="Temps" Foreground="#666" />
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="CPU (%)" Foreground="#666" MaxValue="100" MinValue="0" />
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Border>

                        <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1" Background="White" Margin="8,0,0,0">
                            <lvc:CartesianChart x:Name="RamChart" Height="250" Margin="8">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="Temps" Foreground="#666" />
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="RAM (GB)" Foreground="#666" />
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Border>
                    </Grid>

                    <TextBlock x:Name="SectionClients" Text="Machines connectées" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,12"/>

                    <Border Padding="0" Margin="0,0,0,16" BorderBrush="#DDD" BorderThickness="1" Background="White">
                        <DataGrid x:Name="GridClients" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                                  HeadersVisibility="Column" GridLinesVisibility="None" AlternationCount="2">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Machine" Binding="{Binding MachineName}" Width="*"/>

                                <DataGridTemplateColumn Header="Statut" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border x:Name="StatusPill" CornerRadius="12" Padding="10,2" Background="#F3E5E5">
                                                <TextBlock x:Name="TxtStatus" Text="Hors ligne" Foreground="#B00020" FontWeight="SemiBold"/>
                                            </Border>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                    <Setter TargetName="TxtStatus" Property="Text" Value="En ligne"/>
                                                    <Setter TargetName="TxtStatus" Property="Foreground" Value="#0B8A3B"/>
                                                    <Setter TargetName="StatusPill" Property="Background" Value="#DFF6E6"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Dernière vue" Binding="{Binding LastSeen, StringFormat={}{0:dd/MM/yyyy HH:mm:ss}}" Width="170"/>

                                <DataGridTemplateColumn Header="Actions" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button x:Name="BtnManage" Content="Gérer" Click="BtnManageClient_Click" Padding="8,0" Background="#2980B9" Foreground="White" Height="28"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <TextBlock x:Name="SectionAudit" Text="Logs serveur" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,12"/>
                    <Border Padding="0" BorderBrush="#DDD" BorderThickness="1" Background="#FAFAFA">
                        <ListBox x:Name="LstLogs" Background="#FAFAFA" DisplayMemberPath="." Height="180"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
